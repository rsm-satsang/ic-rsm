// Custom-named intake route for "Create Script for Long form Video" projects.
// Reuses the shared IntakePage; distinct URL + project title provide the naming.
import IntakePage from "./IntakePage";
export default function LongformScriptIntakePage() {
  return <IntakePage />;
}
