// Custom-named intake route for "Create Script for Shorts/Reels" projects.
// Reuses the shared IntakePage; distinct URL + project title provide the naming.
import IntakePage from "./IntakePage";
export default function ShortsScriptIntakePage() {
  return <IntakePage />;
}
